FROM node:20-alpine

WORKDIR /usr/src/app/server

# Install dependencies
COPY server/package*.json ./
RUN npm ci --production

# Copy server source
COPY server/ .

# Generate prisma client if present (no-op if not)
RUN npx prisma generate || true

ENV PORT=5000
EXPOSE 5000

CMD ["node", "index.js"]
