FROM node:20-alpine
WORKDIR /usr/src/app/client

# Install dependencies (use legacy-peer-deps to avoid strict peer conflicts)
COPY client/package*.json ./
RUN npm install --legacy-peer-deps

# Copy client source
COPY client/ .

ENV PORT=3000
EXPOSE 3000

CMD ["npm", "run", "dev"]
